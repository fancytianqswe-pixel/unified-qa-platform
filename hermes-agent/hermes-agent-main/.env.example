# 复制为 .env 后由 docker compose 从「项目目录」加载（默认即 compose 所在目录 hermes-agent-main）。
# 若技能仍为 79 条、无 h1:skill-creator-skill，请取消下行注释并改为本机「仓库根/platform-skills」的绝对路径：
# PLATFORM_SKILLS_BIND=C:/Users/you/Desktop/cursor/platform-skills

# --- 与本仓库 Next 前端（cursor/frontend）联调 ---
# 在 frontend 目录复制 `.env.example` 为 `.env.local`，至少设置：
#   HERMES_GATEWAY_URL=http://127.0.0.1:8642
#   API_SERVER_KEY=<与本 compose 中 API_SERVER_KEY 相同；docker-desktop 默认常为 hermes-local-docker-desktop-dev>
